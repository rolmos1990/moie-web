version: "3"
services:
  siteweb:
    logging:
      driver: "json-file"
      options:
        max-file: 5
        max-size: 10m
    image: 'rolmos/lucy-web:v-0.0.1'
    volumes:
      - /var/catalogo:/var/html/catalog
    ports:
      - "8082:8882"
    environment:
      DB_HOST: localhost
      DB_PORT: 3306
      DB_USERNAME: root
      DB_PASSWORD: Panama2018.
      DB_DATABASE: "moie-lucy-v2"
      HTTP_HOST: "localhost:8082"
      TZ: America/Bogota
